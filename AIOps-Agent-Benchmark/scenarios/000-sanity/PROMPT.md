# Sanity Check — JSON Schema & Baseline Token Measurement

## Task

Run the following kubectl command and present the output as a markdown table:

```bash
kubectl --context AIOps-Agent-Benchmark get nodes -o wide
```

Include these columns in the table: NAME, STATUS, ROLES, AGE, VERSION, INTERNAL-IP, OS-IMAGE.

Do not install anything. Do not modify any cluster resources. Read-only only.
